{
  "scripts": {
    "dev": "nodemon --watch server.js --watch ./public --inspect server.js",
    "start": "NODE_ENV=production node server.js",
    "db": "echo '{\"users\":[],\"messages\":[]}' > db.json",
    "build": "yarn db"
  },
  "dependencies": {
    "cookie-parser": "^1.4.4",
    "express": "^4.16.4",
    "json-server": "^0.14.2",
    "lodash": "^4.17.11",
    "web-push": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
